//TODO: Create a header component
//Todo: Make it reusable with props

export const Header = ({isActive}) => {
    return(
      <header className="w-full h-4 bg-red-200" onClick={() => console.log("Clicked !")}>

      </header>
    );
}